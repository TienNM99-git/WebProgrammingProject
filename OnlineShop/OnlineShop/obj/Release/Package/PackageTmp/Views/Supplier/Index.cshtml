@model IEnumerable<OnlineShop.DBModels.Supplier>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_mainLayout.cshtml";
}

<div class="card-body">
    <form class="d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0">
        <div class="input-group" style="margin-bottom:10px">
            <input type="text" class="form-control" placeholder="Search for..." aria-label="Search" aria-describedby="basic-addon2">
            <div class="input-group-append">
                <button class="btn btn-primary" type="button">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </div>
    </form>
    <div class="table-responsive">
        <table class="table table-bordered" width="100%" cellspacing="0">
            <thead>
                <tr>
                    <th>Supplier ID</th>
                    <th>Supplier name</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Sid</td>
                        <td>@item.SupplierName</td>
                        <td style="width:400px">


                            <a style="display:inline" class="nav-link" href="@Url.Action("Edit", "Supplier", new { id = item.Sid } )">
                                <i class="fas fa-edit"></i>
                                <span>Edit</span>
                            </a>
                            <a style="display:inline" class="nav-link" href="@Url.Action("Delete", "Supplier", new { id = item.Sid })">
                                <i class="fa fa-trash"></i>
                                <span>Delete</span>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <a style="display:block; width:200px;" class="btn btn-primary btn-block" href="@Url.Action("Create", "Supplier")">
            <i class="fas fa-plus"></i>
            <span>Add a new supplier</span>
        </a>
    </div>
</div>